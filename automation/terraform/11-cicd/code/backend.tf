# backend.tf
# S3 远程后端配置
#
# CI/CD 环境需要远程后端来：
# 1. 多人协作时避免状态冲突
# 2. GitHub Actions 运行器无状态，需要持久存储
# 3. 使用 S3 原生锁定防止并发 apply

# =============================================================================
# 后端配置（取消注释并填入实际值）
# =============================================================================
# terraform {
#   backend "s3" {
#     bucket       = "terraform-state-YOUR_ACCOUNT_ID"
#     key          = "cicd-example/terraform.tfstate"
#     region       = "ap-northeast-1"
#     encrypt      = true
#     use_lockfile = true  # Terraform 1.10+ 原生 S3 锁定
#   }
# }

# =============================================================================
# 本地开发说明
# =============================================================================
# 默认使用本地后端（无需配置）
# 用于：本地测试、学习练习
#
# 生产环境请取消上方注释并配置 S3 后端
# 参考：Lesson 02 - 状态管理与远程后端
