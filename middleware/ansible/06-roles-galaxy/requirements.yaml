---
# requirements.yaml - 外部依存管理
# 使用方法: ansible-galaxy install -r requirements.yaml

# Community Roles
roles:
  # geerlingguy の高品質 Roles（例）
  # - name: geerlingguy.docker
  #   version: "6.1.0"
  # - name: geerlingguy.nginx
  #   version: "3.1.0"

  # Git リポジトリから
  # - src: https://github.com/your-org/ansible-role-custom.git
  #   scm: git
  #   version: v1.0.0
  #   name: custom_role

# Ansible Collections
collections:
  # AWS Collection
  - name: amazon.aws
    version: ">=6.0.0"

  # General utilities
  - name: community.general
    version: ">=7.0.0"

  # POSIX modules (firewalld など)
  - name: ansible.posix
    version: ">=1.5.0"
