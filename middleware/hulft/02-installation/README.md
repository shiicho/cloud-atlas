# 02 · HULFT 安装与基本配置

> **目标**：完成 HULFT8 双节点 Lab 环境搭建  
> **前置**：[01 · 网络拓扑与安全基础](../01-network-security/)  
> **适用**：日本 SIer/银行 IT 岗位面试准备  
> **时长**：约 90 分钟  
> **费用**：2x t3.small EC2（约 $0.05/小时）；**完成后记得清理资源**

## 将完成的内容

1. 理解 HULFT Manager vs Engine 的区别
2. 掌握核心配置文件位置和作用
3. 搭建双节点 HULFT8 Lab 环境
4. 完成首次文件传输测试

---

## Step 1 — 理解 HULFT 组件

### HULFT Engine vs HULFT Manager

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         HULFT 组件架构                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                      HULFT Manager (可选)                            │   │
│   │                                                                     │   │
│   │   • Web GUI 管理界面 (端口 25421)                                    │   │
│   │   • 集中配置管理                                                     │   │
│   │   • 可推送定义到多个节点                                              │   │
│   │   • 监控和报表                                                       │   │
│   │                                                                     │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                              │                                              │
│                    推送配置 / 监控                                          │
│                              ↓                                              │
│   ┌──────────────────────┐          ┌──────────────────────┐               │
│   │    HULFT Engine      │          │    HULFT Engine      │               │
│   │      (Node A)        │ ←──────→ │      (Node B)        │               │
│   │                      │  传输    │                      │               │
│   │  • 实际传输进程      │          │  • 实际传输进程      │               │
│   │  • 可独立运行        │          │  • 可独立运行        │               │
│   │  • 处理文件收发      │          │  • 处理文件收发      │               │
│   └──────────────────────┘          └──────────────────────┘               │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 组件对比

| 特性 | HULFT Engine | HULFT Manager |
|------|--------------|---------------|
| **必要性** | 必须 | 可选 |
| **功能** | 实际文件传输 | Web GUI 管理 |
| **独立运行** | 可以 | 需要 Engine |
| **配置方式** | 命令行/配置文件 | Web 界面 |
| **适用场景** | 所有环境 | 中大规模部署 |
| **端口** | 8594, 8500 | 25421 |

### Lab 环境选择

**本课程：只安装 Engine**

```
✅ 本课程方案：2 节点 Engine-Only

   • 学习核心概念足够
   • 资源占用少
   • 快速上手

❌ 不使用 Manager 的原因：
   • 增加复杂度
   • 小规模部署不需要
   • 免费试用版功能受限
```

> 💡 **面试要点 #1**
>
> **问题**：「HULFT ManagerとHULFT Engineの違いは何ですか？」
>
> （中文参考：HULFT Manager 和 HULFT Engine 有什么区别？）
>
> **期望回答**：
> - **Engine**：实际传输守护进程，可独立运行
> - **Manager**：Web GUI 集中配置/监控（可选）
> - Manager 可以推送定义到多个节点
> - 小规模部署通常跳过 Manager

---

## Step 2 — 核心文件位置

### 目录结构

```
/opt/hulft8/                    # 安装根目录
├── bin/                        # 可执行文件
│   ├── hulstart                # 启动 daemon
│   ├── hulstop                 # 停止 daemon
│   ├── hulstat                 # 查看状态
│   └── hulcmd                  # 命令行工具
│
├── hlmtable.tbl               # ⭐ 节点定义表（对端主机）
├── hlmdef.tbl                 # ⭐ 传输定义表
│
├── spool/                      # 队列目录（Store-and-Forward）
│   └── <transfer_id>/          # 每个传输的临时数据
│
├── log/                        # 日志目录
│   ├── hulft.log              # 主日志
│   └── hulevt.log             # 事件日志
│
├── hulenv                      # 环境变量配置
└── hulfkey                     # 许可证文件
```

### 关键配置文件

| 文件 | 用途 | 重要性 |
|------|------|--------|
| **hlmtable.tbl** | 节点定义（对端 IP/端口） | ⭐⭐⭐ |
| **hlmdef.tbl** | 传输定义（送信/受信设置） | ⭐⭐⭐ |
| **hulenv** | 环境变量（路径、语言等） | ⭐⭐ |
| **hulfkey** | 许可证密钥 | ⭐⭐ |

### 日志位置

```bash
# 主日志 - 传输记录、错误信息
/opt/hulft8/log/hulft.log

# 事件日志 - 系统事件
/opt/hulft8/log/hulevt.log

# 常用查看命令
tail -f /opt/hulft8/log/hulft.log
grep "RC=" /opt/hulft8/log/hulft.log | tail -20
```

> 💡 **面试要点 #2**
>
> **问题**：「LinuxでHULFTの定義ファイルはどこにありますか？また、ログはどこに保存されますか？」
>
> （中文参考：在 Linux 上，HULFT 的定义文件在哪里？日志保存在哪里？）
>
> **期望回答**：
> - 定义文件：`/opt/hulft8/hlmtable.tbl`（节点）、`hlmdef.tbl`（传输）
> - 日志：`/opt/hulft8/log/hulft.log`、`hulevt.log`
> - Spool（队列）：`/opt/hulft8/spool`

---

## Step 3 — Lab 环境准备

### 架构概览

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         Lab 环境架构                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│                           VPC (10.0.0.0/16)                                 │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                      Public Subnet (10.0.1.0/24)                     │   │
│   │                                                                     │   │
│   │   ┌─────────────────┐              ┌─────────────────┐             │   │
│   │   │   hulft-node-a  │              │   hulft-node-b  │             │   │
│   │   │   10.0.1.10     │ ←─────────→  │   10.0.1.20     │             │   │
│   │   │                 │   8594/8500  │                 │             │   │
│   │   │   t3.small      │              │   t3.small      │             │   │
│   │   │   Amazon Linux  │              │   Amazon Linux  │             │   │
│   │   └─────────────────┘              └─────────────────┘             │   │
│   │            ↑                                ↑                       │   │
│   │            └────────── SSM 管理 ────────────┘                       │   │
│   │                                                                     │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│   Security Group: TCP 8594, 8500 between nodes                              │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 方案选择

| 方案 | 优点 | 缺点 | 推荐 |
|------|------|------|------|
| **EC2 + HULFT 试用版** | 真实环境 | 需申请试用 | ⭐ 最佳 |
| **Docker 模拟** | 快速、免费 | 功能受限 | 概念学习 |
| **本地 VM** | 离线可用 | 配置复杂 | 备选 |

### 获取 HULFT 试用版

```
申请地址：https://www.hulft.com/trial（日语）

试用版限制：
• 30 天有效期
• 绑定 MAC/主机名
• 吞吐量受限
• 无生产支持

提示：学习足够，但需提前申请
```

---

## Step 4 — 安装前检查清单

### Pre-Install Checklist

在两个节点上执行以下检查：

```bash
# 1. 检查操作系统
cat /etc/os-release

# 2. 检查可用磁盘空间（至少 2GB）
df -h /opt

# 3. 检查内存（至少 1GB 可用）
free -h

# 4. 检查 ulimit（文件描述符）
ulimit -n
# 推荐值：65535

# 5. 检查 locale
locale
# 推荐：en_US.UTF-8 或 ja_JP.UTF-8

# 6. 检查 NTP 同步
timedatectl status
```

### 设置 ulimit

```bash
# 编辑 /etc/security/limits.conf
sudo tee -a /etc/security/limits.conf << 'EOF'
# HULFT file descriptor limits
hulftsvc soft nofile 65535
hulftsvc hard nofile 65535
EOF

# 验证（需重新登录）
su - hulftsvc -s /bin/bash -c "ulimit -n"
```

### 设置 Locale

```bash
# 检查可用 locale
locale -a | grep -E "en_US|ja_JP"

# 设置系统 locale（如需要）
sudo localectl set-locale LANG=en_US.UTF-8

# 验证
locale
```

### 创建服务账户（参考 Lesson 01）

```bash
# 创建组和用户
sudo groupadd -f hulft
sudo useradd -r -s /sbin/nologin -d /opt/hulft -g hulft -c "HULFT Service" hulftsvc

# 创建安装目录
sudo mkdir -p /opt/hulft8
sudo chown hulftsvc:hulft /opt/hulft8
```

---

## Step 5 — 安装 HULFT Engine

### 安装步骤（两个节点都执行）

```bash
# 1. 上传安装包（假设已获得试用版）
# 将 hulft8_linux_x64.tar.gz 上传到 /tmp/

# 2. 解压安装包
cd /tmp
tar xzf hulft8_linux_x64.tar.gz

# 3. 以 root 运行安装程序
sudo ./install.sh

# 4. 按提示操作：
#    - 安装目录：/opt/hulft8
#    - 服务账户：hulftsvc
#    - Control 端口：8594
#    - Data 端口：8500

# 5. 应用许可证
sudo cp /tmp/hulfkey /opt/hulft8/
sudo chown hulftsvc:hulft /opt/hulft8/hulfkey
```

### 配置环境变量

```bash
# 编辑 /opt/hulft8/hulenv
sudo tee /opt/hulft8/hulenv << 'EOF'
# HULFT Environment Configuration
export HULFT_HOME=/opt/hulft8
export PATH=$HULFT_HOME/bin:$PATH
export LANG=en_US.UTF-8
EOF

# 设置权限
sudo chown hulftsvc:hulft /opt/hulft8/hulenv
```

### 启动 HULFT Daemon

```bash
# 启动
sudo -u hulftsvc /opt/hulft8/bin/hulstart

# 检查状态
/opt/hulft8/bin/hulstat

# 期望输出：
# HULFT Status: LISTEN
# Control Port: 8594
# Data Port: 8500
```

---

## Step 6 — 配置节点定义

### 在 Node A 上配置

```bash
# 编辑节点定义表
sudo -u hulftsvc tee /opt/hulft8/hlmtable.tbl << 'EOF'
# HULFT Node Definition Table
# Format: NODE_ID  IP_ADDRESS  CONTROL_PORT

# 本机
NODE_A    10.0.1.10    8594

# 对端
NODE_B    10.0.1.20    8594
EOF
```

### 在 Node B 上配置

```bash
# 编辑节点定义表
sudo -u hulftsvc tee /opt/hulft8/hlmtable.tbl << 'EOF'
# HULFT Node Definition Table
# Format: NODE_ID  IP_ADDRESS  CONTROL_PORT

# 本机
NODE_B    10.0.1.20    8594

# 对端
NODE_A    10.0.1.10    8594
EOF
```

### 验证配置

```bash
# 重启 HULFT 加载新配置
sudo -u hulftsvc /opt/hulft8/bin/hulstop
sudo -u hulftsvc /opt/hulft8/bin/hulstart

# 验证节点定义已加载
/opt/hulft8/bin/hulstat -node
```

---

## Step 7 — 首次传输测试

### 创建测试文件（Node A）

```bash
# 创建测试文件
echo "Hello from Node A - $(date)" > /tmp/test_file.txt

# 确认内容
cat /tmp/test_file.txt
```

### 发送文件（Node A → Node B）

```bash
# 使用 sendfile 进行 ad-hoc 传输（测试用）
/opt/hulft8/bin/hulcmd -sendfile \
  -node NODE_B \
  -file /tmp/test_file.txt \
  -dest /tmp/received_file.txt

# 检查返回码
echo "Return Code: $?"
```

### 验证接收（Node B）

```bash
# 检查文件是否到达
ls -la /tmp/received_file.txt

# 查看内容
cat /tmp/received_file.txt

# 期望输出：
# Hello from Node A - <timestamp>
```

### 检查日志

```bash
# Node A（发送方）
grep "NODE_B" /opt/hulft8/log/hulft.log | tail -5

# Node B（接收方）
grep "NODE_A" /opt/hulft8/log/hulft.log | tail -5

# 查找 RC=0（成功）
grep "RC=0" /opt/hulft8/log/hulft.log | tail -5
```

---

## Step 8 — 常见问题排查

### 问题 1：连接失败

```bash
# 症状
hulcmd: Connection refused

# 排查步骤
# 1. 检查对端 HULFT 是否运行
ssh node-b "/opt/hulft8/bin/hulstat"

# 2. 检查网络连通性
nc -zv 10.0.1.20 8594

# 3. 检查防火墙规则
sudo iptables -L -n | grep 8594
```

### 问题 2：权限错误

```bash
# 症状
Permission denied: /opt/hulft8/spool

# 解决
sudo chown -R hulftsvc:hulft /opt/hulft8/spool
sudo chmod 755 /opt/hulft8/spool
```

### 问题 3：许可证错误

```bash
# 症状
License expired or invalid

# 检查
cat /opt/hulft8/hulfkey
# 确认许可证未过期、主机名匹配
```

### 问题 4：文件描述符不足

```bash
# 症状
Too many open files

# 检查当前限制
cat /proc/$(pgrep hulft)/limits | grep "open files"

# 解决：增加 ulimit（见 Step 4）
```

---

## 实践练习

### 练习：双向传输测试

**目标**：验证双向通信正常

```
1. 从 Node A 发送文件到 Node B ✓（已完成）
2. 从 Node B 发送文件到 Node A
3. 检查两边日志都显示 RC=0
```

<details>
<summary>点击查看参考命令</summary>

```bash
# 在 Node B 上执行
echo "Hello from Node B - $(date)" > /tmp/test_from_b.txt

/opt/hulft8/bin/hulcmd -sendfile \
  -node NODE_A \
  -file /tmp/test_from_b.txt \
  -dest /tmp/received_from_b.txt

# 在 Node A 上验证
cat /tmp/received_from_b.txt
```

</details>

---

## 常见错误

| 错误 | 后果 | 预防 |
|------|------|------|
| 忘记 ulimit | 高负载下 "too many open files" | 安装前配置 65535 |
| Locale 为 C | SJIS 文件名乱码 | 设置 en_US.UTF-8 |
| 不备份定义 | 升级后无法回滚 | 每次变更前备份 |
| Spool 磁盘小 | 重试时磁盘满 | 监控磁盘使用 |

> 💡 **面试要点 #3**
>
> **问题**：「2ノードのHULFT8ラボ環境を構築する最小限の手順を説明してください。」
>
> （中文参考：请概述搭建 2 节点 HULFT8 Lab 环境的最小步骤。）
>
> **期望回答**：
> 1. 创建 hulft 用户/组（非登录）
> 2. 设置 ulimit、locale、NTP 同步
> 3. 开放防火墙端口 8594/8500
> 4. 两节点安装 Engine
> 5. 应用许可证，配置互相的节点定义
> 6. 用 `hulcmd -sendfile` 测试，检查日志 RC=0

---

## 清理资源

**Lab 完成后，记得清理 EC2 实例避免费用：**

```bash
# 如果使用 CloudFormation
aws cloudformation delete-stack --stack-name hulft-lab

# 如果手动创建
aws ec2 terminate-instances --instance-ids <instance-id-a> <instance-id-b>
```

---

## 小结

| 主题 | 要点 |
|------|------|
| 组件 | Engine（必须）vs Manager（可选） |
| 文件位置 | hlmtable.tbl、hlmdef.tbl、spool、log |
| 安装前 | ulimit、locale、NTP、服务账户 |
| 验证 | hulstat、hulcmd -sendfile、检查 RC=0 |

---

## 下一步

完成本课后，请继续：

- **[03 · 字符编码处理（SJIS↔UTF-8/EBCDIC）](../03-encoding/)** — 处理日本常见的编码转换场景

---

## 系列导航 / Series Nav

| 课程 | 主题 |
|------|------|
| 00 · 概念与架构 | Store-and-Forward, 术语 |
| 01 · 网络与安全 | 端口、防火墙、服务账户 |
| **02 · 安装配置** | ← 当前课程 |
| 03 · 字符编码 | SJIS↔UTF-8, EBCDIC |
| 04 · 集信/配信实战 | 传输组、重试机制 |
| 05 · 作业联动 | JP1 集成、日志分析 |
| 06 · 云迁移 | HULFT Square, AWS VPC |
